//
//  Colors.swift
//  Split Rewards
//
//

import SwiftUI

extension Color {
    // MARK: - Brand

    static let splitBrandBlue = Color(red: 19 / 255, green: 43 / 255, blue: 98 / 255)
    static let splitBrandPink = Color(red: 190 / 255, green: 50 / 255, blue: 135 / 255)
    static let splitBrandWhite = Color(white)

    // MARK: - App palette

    static let splitBerry = Color(red: 153 / 255, green: 20 / 255, blue: 107 / 255)
    static let splitIndigo = Color(red: 43 / 255, green: 57 / 255, blue: 122 / 255)
    static let splitDarkRed = Color(red: 160 / 255, green: 20 / 255, blue: 60 / 255)
    static let splitDeepRed = Color(red: 120 / 255, green: 0 / 255, blue: 32 / 255)

    // MARK: - Surfaces

    static let splitAppBlack = Color(red: 5 / 255, green: 5 / 255, blue: 8 / 255)
    static let splitSurface = Color(red: 18 / 255, green: 22 / 255, blue: 44 / 255)
    static let splitSurfaceRaised = Color(red: 28 / 255, green: 34 / 255, blue: 62 / 255)
    static let splitCardSurface = Color(red: 20 / 255, green: 24 / 255, blue: 48 / 255)
    static let splitCardSurfaceRaised = Color(red: 30 / 255, green: 36 / 255, blue: 68 / 255)

    static let splitSoftBackground = Color(white: 0.08)
    static let splitInputSurface = Color(white: 0.12)
    static let splitInputSurfaceSecondary = Color(white: 0.10)
    static let splitInputSurfaceTertiary = Color(white: 0.13)
    static let splitFieldSurface = Color(white: 0.16)
}
